# festival

Descreva aqui as alterações/correções que fez
1. ordenar a lista dos dias no queryset do django
2. a linha 'ordering = ["dia__data", "hora"]' serve para o django ordenar internamente a lista de objetos a retornar num queryset (penso que isto seja uma alternativa para o exercicio anterior...)
3. adicionar os restantes campos no forms.py
4. criar uma route no urls.py, criar uma view e implementar o form
5. criar uma view e url e template
6. adicionar o field acessibilidade_<...> ao model Palco e ao form PalcoForm. editar o html para mostrar os dados e mostrar dinamicamente o icone consoante o estado da variavel (ifblocks)
7. criar route no urls.py e view. o template já estava implementado...
