# festival

Descreva aqui as alterações/correções que fez
1. ordenar a lista dos dias no queryset do django
2. a linha 'ordering = ["dia__data", "hora"]' serve para o django ordenar internamente a lista de objetos a retornar num queryset (penso que isto seja uma alternativa para o exercicio anterior...)
3. 
